export const environment = {
  // Relative path — the Angular dev server proxies /api to the backend
  // (see proxy.conf.json), so the browser only ever talks to one origin.
  // That's what lets a single tunneled URL serve both frontend and backend.
  apiUrl: '/api',
};
